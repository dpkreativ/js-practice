// Learn about JavaScript Set
let school = { school: 'MECDA' };

const trySet = new Set();

trySet.add('Hello');
trySet.add('Bye');

trySet.add(school);
trySet.delete(school);

console.log(trySet);

// SET SYNTAX
// new Set()
